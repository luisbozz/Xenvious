<UserControl x:Class="Xenvious.WeaponInventoryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Height="300" Width="215" >

    <Border BorderThickness="1" CornerRadius="5" BorderBrush="{DynamicResource SectionBackgroundBrush}" Background="{DynamicResource SectionBackgroundBrush}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="8" ShadowDepth="0"/>
        </Border.Effect>
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="0.13*" MinHeight="50" MaxHeight="100"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!--Top-->

            <Border Grid.Row="0" CornerRadius="5" Background="{DynamicResource SeactionHeaderBackgroundBrush}" Margin="8,5,8,0">
                <Viewbox VerticalAlignment="Stretch" HorizontalAlignment="Center">
                    <Label Foreground="{DynamicResource TextColor}" FontFamily="{DynamicResource Volte}" FontWeight="Bold" Content="{Binding HeaderText, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"/>
                </Viewbox>
            </Border>

            <!--Bottom-->
            <Border Grid.Row="1" CornerRadius="5" Background="{DynamicResource SeactionHeaderBackgroundBrush}" Margin="8">
                <ScrollViewer Margin="5" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="weaponList" Background="Transparent" >

                    </StackPanel>
                </ScrollViewer>
            </Border>

        </Grid>
    </Border>

</UserControl>
